# Virtual Item Server
This repository contains code for the Enjin-backed virtual item management server.

## Configuration

To configure this application, copy the `.sample-env` to `.env` and insert all relevant credentials.
